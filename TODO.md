# Backend Implementation TODO

## Plan Steps (Approved)
- [ ] 1. Create backend/ folder with all files (package.json, server.js, routes/, controllers/, middleware/)
- [ ] 2. cd backend && npm install
- [ ] 3. Test server: node server.js, curl /api/config, POST config, upload image
- [ ] 4. Update README.md with instructions
- [ ] 5. Update frontend JS files to use APIs (admin-editor.js, image-uploader.js, load-site.js)
- [ ] 6. Test full flow: admin edit/save, image upload, site preview
- [ ] 7. EC2 deploy prep (PM2, nginx notes)

✅ 1. Create backend/ folder with all files (package.json, server.js, routes/, controllers/, middleware/)

✅ 2. cd backend && npm install

✅ 3. Test server: node server.js (run manually, paste output)

✅ 4. Update README.md with instructions

✅ 3. Test server: node server.js (run manually, paste output) → Server running OK (static served, APIs ready)

**Next: Step 5**
